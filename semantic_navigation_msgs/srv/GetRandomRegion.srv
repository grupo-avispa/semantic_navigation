# Request / response service to get a random region

---
string region_name # Name of the region
bool success # True if a random region could be selected
string message # Human-readable reason when success is false

# Constants
string UNKNOWN = unknown
