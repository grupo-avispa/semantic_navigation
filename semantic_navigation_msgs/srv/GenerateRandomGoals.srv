# Request / response service to generate random goals in regions determined by names

int32       n            # Number of goals
string      region_name  # Name of the region
float32     yaw          # Yaw of the goals. Value are valid between -PI and PI radians.
string      orientation  # Random orientation of the goals. Can be towards 'outside' the region, 'inside' the region, the requested or 'random' (default)
float32     border       # Distance from the edge of the region. Default: 0.0

# Type of the direction
string OUTSIDE = outside
string INSIDE = inside
string RANDOM = random
string REQUESTED = requested
---
nav_msgs/Goals goals # Goals. May contain fewer than `n` poses if the region is hard to sample
bool success # True if the request was valid and processed (regardless of how many goals fit)
string message # Human-readable reason when success is false
